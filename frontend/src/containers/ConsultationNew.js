import React from 'react';
import EditSingle from '../components/Consultation/edit'

const ConsultationNew = props => {
  return (
    <EditSingle showPrinter={true} isNew={true}/>
  );
};

export default ConsultationNew